<?php
declare(strict_types=1);

namespace UriInterop\Interface;

/**
 * This interface marks implementations where the component values are expected
 * to consist of characters from the Universal Character Set (Unicode/ISO 10646)
 * as per RFC 3987, instead of only ASCII characters.
 */
interface IriEncoded
{
}
